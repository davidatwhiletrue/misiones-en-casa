services:
  app:
    build: .
    ports:
      - "3030:3030"
    volumes:
      - app-data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/data/dev.db
    restart: unless-stopped

volumes:
  app-data:
