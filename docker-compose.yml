services:
  app:
    build: .
    ports:
      - "3030:3030"
      - db
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL}
    restart: unless-stopped

volumes:
  mysql-data:
